from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A programmable digital assistant that can ingest text, video, images and other assistants to ask questions about, summarize and be creative.',
    author='NCategory',
    license='',
)
